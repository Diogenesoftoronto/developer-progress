var addBinary = function(a, b) {
  const sum = (a, b) => {
      let storeBit;
      const sumArray = a.reduce((acc, element, index)=> {
          let elementfromA = element;
          let elementfromB = b.charAt(index);
          (elementfromA && !elementfromB) || (!elementfromA && elementfromB) || (elementfrom)? acc.push(parseInt(elementfromA)) : 
          elementfromA && elementfromB ? acc.push(0) : 
      }, []) 
     // const arrA = a.split('')
     // const arrB = b.split('')
      // parseInt(a) > parseInt(b) ? 
  }
};